{
  "name": "@observability/core",
  "version": "1.0.0",
  "private": true,
  "description": "Core SDK for mobile observability platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "lint": "eslint src --ext .ts"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@observability/config": "1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  }
}
